import { AddressbarColor, SessionStorage } from 'quasar'

export default () => {
  // console.log("lll")
  AddressbarColor.set('#fff')
  // -------------------------
  // // Bazed is a key for knowing if the home page data is loaded in session
  // let sessionValue = SessionStorage.getItem('Bazed')
  // if (sessionValue==false) {
  //   SessionStorage.set('Bazed', true)
  // }
}
